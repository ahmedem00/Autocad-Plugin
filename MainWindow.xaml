<Window x:Class="Dimsion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Dimsion"
        mc:Ignorable="d" 
        Title="Dimension Grid Generator" 
        WindowStartupLocation="CenterScreen"
        Height="450" Width="800"
        MinHeight="400" MinWidth="600">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0066B3"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004D8C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Border CornerRadius="5" 
                BorderThickness="1" BorderBrush="#FF8A7676"
                Margin="30" Padding="0" Background="#FFC7B7CE">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Grid Dimension Settings" 
                               FontSize="18" FontWeight="Bold"
                               Margin="0,0,0,15" Foreground="#333333"/>

                    <StackPanel Grid.Row="1" Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="X Axis Count:" Width="120"/>
                            <TextBox x:Name="xAxisInput" Width="200" 
                                     ToolTip="Enter the number of columns for the grid"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="Y Axis Count:" Width="120"/>
                            <TextBox x:Name="yAxisInput" Width="200" 
                                     ToolTip="Enter the number of rows for the grid"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="Spacing:" Width="120"/>
                            <TextBox x:Name="spacingInput" Width="200" 
                                     ToolTip="Enter the spacing between grid elements"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="Layer:" Width="120"/>
                            <TextBox x:Name="layerNameInput" Width="200" 
                            ToolTip="Enter the layer name"/>
                        </StackPanel>
                    </StackPanel>

                    <Button x:Name="button" Content="Create Grid with Dimension" Grid.Row="2"
                            HorizontalAlignment="Center" 
                            Click="button_Click"/>
                </Grid>

                <!-- Image placed in its own column on the right -->
                <Image Grid.Column="1" 
                       Source="/Demo00;component/Resources/ITI.png" Height="166"
                       Margin="5,0,0,0"
                       VerticalAlignment="Top"/>
            </Grid>
        </Border>
    </Grid>
</Window>